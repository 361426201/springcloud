server:
  port: 8400
spring:
  application:
    name: cloud-zuul
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册中心地址
zuul:
  routes: #路由
    user:  #随意命名，用于区分多个路由的唯一标识
      path: /user/**   #需要被转发的规则
      serviceId: cloud-user   #需要被转发的服务名称


